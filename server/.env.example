# Copy to .env and set a password. SQL Server wants 8+ characters from three
# of: uppercase, lowercase, digits, symbols.
#
# .env is gitignored. Whatever you put here must also be given to the API, which
# reads its connection string from user secrets - see Server Setup in the README.
MSSQL_SA_PASSWORD=
MSSQL_HOST_PORT=1433
